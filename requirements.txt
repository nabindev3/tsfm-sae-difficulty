# Pinned to the versions the committed results were generated against. The
# numerics-critical packages (numpy / scipy / scikit-learn / torch) determine
# the paired-bootstrap and liblinear bit pattern, and the CRN-based ΔCRPS null
# is only reproducible against a fixed numpy/scipy — so these are exact pins,
# not floors. Bump deliberately and re-run reproduce.sh to regenerate the CIs.
torch==2.12.0
chronos-forecasting==2.2.2
datasets==4.8.5
safetensors==0.7.0
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
pyarrow==24.0.0
tqdm==4.67.3
statsmodels==0.14.6  # ADF p-value + autocorrelation for the 8 classical TS stats;
                     # imported lazily inside probing/probe.compute_input_stats
pytest==9.0.3        # dev: runs the offline suite under tests/ (`pytest tests/ -q`)
# Shared core (probe ladder / TopK SAE / cascade / calibration) is the sibling
# fm-difficulty-probe package. Install it editable AFTER the above, with --no-deps
# (this file already covers the runtime stack):
#     pip install -e ../fm-difficulty-probe --no-deps
